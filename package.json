{
  "scripts": {
	"dev": "env $(cat .env | grep -v '^#' | xargs) node index.js",
	"addlink": "env $(cat .env | grep -v '^#' | xargs) node scripts/add_link.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.1.2"
  }
}
